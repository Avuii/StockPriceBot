name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy demo app
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Install shared dashboard dependencies
        working-directory: frontend/dashboard
        run: npm ci --no-audit --no-fund

      - name: Install demo dependencies
        working-directory: demo
        run: npm install --no-audit --no-fund

      - name: Build demo app
        working-directory: demo
        env:
          VITE_API_URL: "demo://mock-api"
          VITE_BASE_PATH: "/${{ github.event.repository.name }}/demo/"
        run: npm run build

      - name: Prepare Pages site
        shell: bash
        run: |
          set -euo pipefail

          rm -rf _site
          mkdir -p _site

          cp -R demo/dist _site/demo

          cat > _site/index.html <<'HTML'
          <!doctype html>
          <html lang="en">
            <head>
              <meta charset="utf-8" />
              <meta name="viewport" content="width=device-width, initial-scale=1" />
              <meta http-equiv="refresh" content="0; url=./demo/" />
              <title>StockPriceBot Demo</title>
              <style>
                body {
                  margin: 0;
                  min-height: 100vh;
                  display: grid;
                  place-items: center;
                  background: #f4f7fb;
                  color: #07152f;
                  font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
                }
                a {
                  color: #1677c8;
                  font-weight: 700;
                }
              </style>
            </head>
            <body>
              <p>Opening <a href="./demo/">StockPriceBot demo</a>.</p>
            </body>
          </html>
          HTML

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
